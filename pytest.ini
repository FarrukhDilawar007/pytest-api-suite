[pytest]
pythonpath = .
testpaths = tests
markers =
    smoke: critical happy-path smoke tests (run on every PR)
    regression: full regression suite (run on merge to main)
    contract: Pact consumer-driven contract tests
    bdd: BDD Gherkin scenario tests
addopts = -v --strict-markers
